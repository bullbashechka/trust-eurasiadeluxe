# Проверка локального сайта — 8 сентября 2026

## Выполнено

- `bun run check`: 0 ошибок, предупреждений и подсказок.
- `bun run test`: 8 успешных тестов — данные, медиа, математические границы скролла, WhatsApp, выборочная загрузка видео, reduced motion, ошибка/повтор/фотографии и смена режима.
- `bun run build`: собраны 5 статических страниц.
- `bun run --cwd website test:build`: проверены внутренние ссылки и ресурсы всех страниц, один H1, описания, Open Graph/X и отсутствие фиктивного WhatsApp.
- Полностью декодированы 54 производных MP4 (27 сцен × 2 размера), ошибок нет. См. `MEDIA_VALIDATION.json`.
- Chrome: пройден каталог и открыты все три квартиры, облачный переход завершает навигацию, прямые страницы и возврат назад работают.
- Chrome, 36,67 м²: выбор гостиной переключил на сцену 03/8; прокрутка увеличила время видео с 0 до 1,270196 с, остановка сохранила время, обратная прокрутка уменьшила его до 0,453641 с. Видео остаётся paused в режиме скролла.
- Обычное воспроизведение запускает текущий ролик и проходит последовательность до последней сцены. Проверен возврат к скроллу.
- Размер 390 × 844: загружаются версии `-mobile.mp4`, интерьер отображается целиком, подписи под изображением. Выявленное переполнение блока характеристик исправлено: ширина контента и окна 390 px. В каталоге нет элементов video.
- Safari на macOS: открылась страница 36,67 м², визуально проверены интерьер, боковая навигация и управление. Полная проверка взаимодействий не завершена: инструмент управления окном возвращал `noWindowsAvailable` и ошибку ScreenCaptureKit. Это ограничение проверки, не установленная ошибка сайта.

## До публичного запуска

- Проверить прокрутку, переходы и обычное воспроизведение в Safari вручную, включая реальный iPhone. Эмуляция ширины в Chrome не проверяет декодер и поведение iOS.
- На целевом хостинге проверить MP4 Range/206, медленную сеть и ограниченный кэш. Ошибки загрузки и reduced motion сейчас покрыты тестами компонента; реальные условия медленной мобильной сети не имитировались.
- Подтвердить телефон WhatsApp, окончательный домен и рекламную информацию комплекса. Задать переменные и пересобрать сайт, затем проверить абсолютные OG/X/canonical URL и WhatsApp.
- Ролик 39-87/07 сохранён целиком. В исходном аудите отмечено более заметное движение: оценить субъективный темп на реальном устройстве до публикации.

## Состояние передачи

Код расположен в `website/`, исходники сохранены. Производные файлы в `website/public/media/` и сборка в `website/dist/` доступны локально; воспроизводятся по README и не добавляются в Git. Публикация не выполнялась. Контактная кнопка появится после настройки подтверждённого номера.
